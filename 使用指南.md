# CloudEye 使用指南

> 一份写给「第一次用」的人的说明。看完你就知道每个监控模式该在什么场景用、该怎么填。

---

## 一、先搞懂一件事：主动 vs 被动

CloudEye 有两类完全不同的工作方式，**这是理解全部功能的钥匙**。

### 🟢 主动监控 —— CloudEye 定时「去戳」目标

CloudEye 每隔一段时间，自己跑去访问你的网站 / 端口 / 接口，看它还活着没。

- 就像你**每隔几分钟给朋友打个电话**，打得通就是正常，打不通就是出事了。
- 目标那边**什么都不用装、不用配**，只要能被访问到就行。
- 适合：网站、API、服务器端口、需要定时执行的任务。

### 🔵 被动接收 —— 别的系统「主动来告诉」CloudEye

CloudEye 自己不动，安安静静等着。是**别的监控系统（Komari、哪吒）在出事时，主动把消息推过来**。

- 就像你**不打电话，而是让朋友出事时自己给你发消息**。
- 需要你**去那个系统的后台，填一个 CloudEye 给的推送地址**（一次性配置）。
- 适合：你已经在用 Komari 探针 / 哪吒面板，想把它们的告警汇总到这里、转发到 Telegram。

> 一句话记忆：
> **主动 = 我去查它；被动 = 它来告诉我。**

---

## 二、5 分钟上手：添加第一个监控（HTTP）

最简单、最常用的就是 HTTP 监控，先从它开始。

1. 登录后，点右上角 **「添加监控」**。
2. **监控名称**：随便起，比如 `我的博客`。
3. **监控类型**：选 `HTTP 检测`（默认就是它）。
4. **监控地址 URL**：填你要监控的网址，比如 `https://example.com`。
5. **检查频率**：默认「固定周期 5 分钟」就够用了。
6. 下面的高级选项**全都不用管**，直接拉到底点保存。

搞定。回到主页就能看到这个监控卡片，绿色是正常，红色是故障。

> 想收到故障提醒？往下看第四节「配置 Telegram 通知」。

---

## 三、六种监控模式详解

按「主动 / 被动 / 混合」分成三组。**不确定用哪个，90% 的情况选 HTTP 就对了。**

### 🟢 主动组（CloudEye 去查）

#### 1. HTTP 检测 —— 最常用
**干什么**：定时访问一个网址，检查它是否正常返回。
**适合**：网站、网页、API 接口、后台管理页。
**怎么填**：
- 监控地址填完整 URL（带 `http://` 或 `https://`）。
- 如果只想知道「网站开没开」，其他都不用动。
- 进阶：可以指定「期望状态码」（默认 200 等即为正常），或填「期望关键词 / 禁止关键词」——页面里出现某段文字才算正常，或出现某段文字（比如「502 Bad Gateway」）就算故障。

#### 2. TCP 连通性检测（Ping）
**干什么**：定时连接一个「主机 + 端口」，看端口通不通。
**适合**：数据库、SSH、游戏服、任何不是网页但要看端口活没活的服务。
**怎么填**：地址填 `域名:端口` 或 `IP:端口`，比如 `db.example.com:3306`。

#### 3. 定时触发（Webhook / Cron）
**干什么**：CloudEye 按你设定的时间，**主动去调用一个外部接口**。它不是「监控」，而是「定时帮你执行一件事」。
**适合**：定时签到、定时保活、定时触发一个 GitHub Action、每天定点跑一个任务。
**怎么填**：
- 监控地址填要触发的 Webhook 地址。
- 检查频率里可以选 **「每日时段随机」**——比如每天 03:00~08:00 之间随机挑一个时间执行一次，适合「一天签到一次」这种场景，随机时间还能避免撞车。

---

### 🔵 被动组（它来告诉 CloudEye）

> ⚠️ 这两种模式**监控地址（URL）不用填**，因为不是 CloudEye 去访问谁，而是等对方推送。
> 配置分两步：① 在本站顶栏 **📡 设置** 里启用接收、拿到推送地址；② 把地址填到 Komari / 哪吒的后台。

#### 4. Komari Webhook 监控
**干什么**：接收 Komari 探针面板推来的通知，掉线时告警 / 转发。
**怎么配**：
1. 点顶栏 **📡** 按钮 → 启用 Komari 通知接收，填上要转发到的 Telegram 群组 ID。
2. 在同一处复制**带 Token 的 Komari Webhook 地址**，粘贴到你的 **Komari 面板**的通知设置里。
3. 回到添加监控，填「监控目标服务器名称」——必须和 Komari 里的服务器名对得上，这样推送来时才能匹配到这张卡片。

#### 5. 哪吒（Nezha）Webhook 监控
**干什么**：同上，接收哪吒面板推来的告警。
**怎么配**：
1. 顶栏 **📡** → 确保已启用 Nezha 通知接收。
2. 在哪吒面板添加「Webhook」通知方式，地址用本站 📡 里复制的**带 Token 的 Nezha Webhook 地址**。
3. 添加监控时填的「服务器名称」，必须和哪吒面板里显示的名称**完全一致**。

---

### 🟣 混合组（先主动触发，再被动等回复）

#### 6. 反馈联动监控（Feedback Linkage）
**干什么**：CloudEye 先**主动触发**一个外部任务（比如触发一个 GitHub Actions 工作流去跑脚本），脚本执行完，再**主动把结果回调**报告给 CloudEye。是「触发 → 执行 → 回执」的完整闭环。
**适合**：需要「按剩余时间智能续期」的场景——例如某个服务有效期 30 天，你想在快到期时（而不是每天）才去续一次；脚本跑完把「还剩多少秒」告诉 CloudEye，CloudEye 就能算出下次该在什么时候再触发。

这是本项目最强大的模式。因为要讲清楚，单独放在第四节详解 👇

---

## 四、【重点】反馈联动 + GitHub Actions 实战

这一节手把手教你把 CloudEye 和 GitHub Actions 接起来，做一个「智能续期」闭环。

### 4.1 先理解闭环的两个方向

反馈联动有**两条数据流**，别搞混：

```
① 触发（CloudEye → GitHub）
   到了该续期的时间点，CloudEye 主动调用 GitHub API，
   启动你仓库里的一个 workflow。

② 回调（你的脚本 → CloudEye）
   workflow 跑完，脚本用 curl 把「还剩多少秒」POST 回 CloudEye，
   CloudEye 据此算出下一次触发时间。
```

CloudEye 靠这两个参数决定「什么时候触发下一次」：

- **续期触发阈值**：剩余时间低于这个值就该续期了。比如填 `24`（小时），意思是「剩余不足 24 小时就触发续期」。
- **执行波动范围（Min~Max）**：在阈值基础上random一个提前量，避免每次都卡在同一时刻。比如填 `1` 至 `3`，实际触发点 = `阈值 - random(1,3)` = 剩余 21~23 小时之间的某一刻触发。

> 计算逻辑（脚本作者可不管，了解即可）：
> `触发点 = 阈值 - random(波动Min, 波动Max)`。
> 脚本回调的 `remaining_time` 若**高于**触发点 → CloudEye 继续等到触发点；若**已低于**触发点 → 按检查间隔尽快触发续期。

---

### 4.2 CloudEye 这边怎么填

添加监控 → 类型选 **`反馈联动监控`**，然后：

| 字段 | 填什么 | 示例 |
|------|--------|------|
| 监控名称 | 随便起 | `我的服务续期` |
| 服务器匹配关键词 | **唯一标识**，回调时用它认领结果 | `my-service` |
| 续期触发阈值（小时） | 剩余低于多少小时触发 | `24` |
| 执行波动范围 | 提前量随机区间 | `1` 至 `3` |
| **触发 URL** | GitHub 的 dispatch 接口 | `https://api.github.com/repos/你的用户名/你的仓库/dispatches` |
| Request Method | 固定 | `POST` |
| Content-Type | 固定 | `application/json` |
| Custom Headers | 见下方 | GitHub Token 等 |
| Request Body | 见下方 | 触发事件名 |

**Custom Headers**（触发 GitHub API 必须带认证，填这三行）：

```
Authorization: Bearer ghp_你的GitHubToken
Accept: application/vnd.github+json
X-GitHub-Api-Version: 2022-11-28
```

> GitHub Token 在 https://github.com/settings/tokens 生成。用 Fine-grained token 的话，给目标仓库的 **Contents: Read and write** 权限即可；用 Classic token 勾 `repo` 就行。

**Request Body**（告诉 GitHub 触发哪个事件）：

```json
{
  "event_type": "cloudeye-renew"
}
```

`event_type` 的值随便起，但要和下面 workflow 里的 `types` 对上。

保存后，CloudEye 就会在算出的触发点自动 POST 这个请求，启动你的 workflow。

---

### 4.3 GitHub 这边：workflow 只管跑脚本，回调写在脚本里

**推荐做法**：把回调 CloudEye 的 `curl` 直接写进你的续期脚本结尾，而不是塞在 workflow 的 yml 里。

为什么这样更好：

- 剩余秒数（`remaining_time`）是你脚本自己算出来的，脚本手里就有——在脚本里直接回调，不用再把这个值从脚本「导出」回 yml 层（否则得 echo 到 `$GITHUB_OUTPUT` 再传，很绕）。
- 脚本自包含，**本地、cron、任何 CI 都能跑**，不绑死在 GitHub Actions 上。
- yml 保持极简，只做两件事：跑脚本 + 把回调地址作为环境变量传进去。

**① workflow：`.github/workflows/renew.yml`**

```yaml
name: CloudEye 续期任务

# 关键：接收 CloudEye 发来的 repository_dispatch 事件
on:
  repository_dispatch:
    types: [cloudeye-renew]     # 必须和 CloudEye Body 里的 event_type 一致
  workflow_dispatch:            # 顺便允许手动点按钮测试

jobs:
  renew:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: 执行续期脚本
        env:
          # 把回调地址作为环境变量传给脚本，脚本内部用它回调
          CLOUDEYE_CALLBACK_URL: ${{ secrets.CLOUDEYE_CALLBACK_URL }}
        run: bash renew.sh
```

**② 续期脚本：`renew.sh`（回调逻辑在这里）**

```bash
#!/usr/bin/env bash
set -euo pipefail

SERVER_NAME="my-service"   # 必须和 CloudEye 里的「匹配关键词」一致

# ===== 1. 这里放你真正的续期逻辑 =====
# ... 执行续期 ...
# 续期成功后，脚本自己算出「剩余有效期秒数」
# 下面用固定值演示：30 天 = 2592000 秒
REMAINING=2592000

# ===== 2. 脚本结尾直接回调 CloudEye =====
curl -X POST "$CLOUDEYE_CALLBACK_URL" \
  -H "Content-Type: application/json" \
  -d "{
    \"server_name\": \"${SERVER_NAME}\",
    \"remaining_time\": ${REMAINING},
    \"status\": \"success\",
    \"message\": \"续期完成\"
  }"
```

**要改成你自己的**：

1. **`SERVER_NAME`** 必须和 CloudEye 里填的「服务器匹配关键词」一致（这里是 `my-service`）。
2. **`REMAINING`** 换成你脚本真实算出的剩余秒数。
3. **回调地址**存成仓库 Secret，别写死：到仓库 `Settings → Secrets and variables → Actions → New repository secret`，名字填 `CLOUDEYE_CALLBACK_URL`，值就是 CloudEye 反馈联动表单里那个**脚本回调 URL**（形如 `https://你的域名/api/callback?token=xxxxxx`，点 📋 复制）。

> 脚本失败（续期没成功）时，可以把 `status` 改成 `fail` 再回调，CloudEye 会把这次标记为故障并推送告警。用 `set -e` 时记得在失败分支里手动发一条失败回调，否则脚本中断就不会回调了。

---

### 4.4 回调 Body 字段说明

脚本 POST 回 CloudEye 时，body 支持这几个字段：

| 字段 | 必填 | 类型 | 说明 |
|------|------|------|------|
| `server_name` | ✅ | 字符串 | 匹配关键词，必须和监控里填的一致，否则回调找不到对应监控（返回 404） |
| `remaining_time` | 推荐 | 数字（**秒**） | 剩余有效期秒数。CloudEye 靠它算下次触发时间 |
| `status` | 可选 | 字符串 | `success` / `up` 视为成功，其他视为失败 |
| `message` | 可选 | 字符串 | 自定义备注，会显示在卡片和 TG 通知里 |

**成功/失败怎么判定**：
- 填了 `status` → 按 `status` 判（`success`/`up` = 成功）。
- 没填 `status` → 看 `remaining_time > 0` 就算成功。

**最小可用回调**（只要这两个字段就能跑）：

```bash
curl -X POST "https://你的域名/api/callback?token=xxxxxx" \
  -H "Content-Type: application/json" \
  -d '{"server_name": "my-service", "remaining_time": 2592000}'
```

---

### 4.5 联调排错

| 现象 | 原因 / 解法 |
|------|------------|
| 回调返回 `404 未匹配到...` | `server_name` 和监控里的「匹配关键词」对不上，检查有没有写错、多空格 |
| 回调返回 `401` / 鉴权失败 | 回调 URL 里的 `token` 不对，重新从表单复制完整地址 |
| GitHub workflow 没被触发 | ① 触发 URL 仓库名/用户名写错；② Token 权限不足或过期；③ Body 的 `event_type` 和 workflow 的 `types` 不一致 |
| workflow 跑了但卡片没更新 | 脚本里的回调 `curl` 失败了，去 Actions 日志看 curl 那步的输出 |
| 想立即测一次 | 到 GitHub 仓库 Actions 页，选这个 workflow 点 `Run workflow`（靠 `workflow_dispatch` 那行支持手动触发） |

---

## 五、配置 Telegram 通知（收故障提醒）

想在服务出问题时收到手机推送，就配一下 Telegram：

1. 找 [@BotFather](https://t.me/BotFather) 建一个自己的机器人，拿到 **Bot Token**。
2. 顶栏 **📡 设置** → 填入 Bot Token。
3. 把你的机器人拉进一个群，或直接和它私聊，拿到 **Chat ID**（可用 [@userinfobot](https://t.me/userinfobot) 获取）。
4. 在对应监控 / 通知设置里填上 Chat ID，出故障时就会推消息过来。

> 反馈联动 / 定时触发模式还支持在配置里单独填一个「TG 通知群组」，每次任务执行（无论成败）都会推一条带「立即重试」按钮的消息。

---

## 六、常见疑问

**Q：我到底该选哪个类型？**
A：监控网站 / API → HTTP；监控端口（数据库、SSH 等）→ TCP；单纯定时跑任务 → 定时触发；要「触发任务并拿回执智能续期」→ 反馈联动；已经在用 Komari / 哪吒想汇总告警 → 对应的 Webhook 模式。拿不准就用 HTTP。

**Q：定时触发 和 反馈联动 有什么区别？**
A：定时触发是「到点就发，发完不管结果」，固定节奏；反馈联动是「根据脚本回报的剩余时间，动态决定下次什么时候发」，适合按有效期续期。

**Q：被动模式（Komari / 哪吒）为什么监控地址不用填？**
A：因为不是 CloudEye 去访问它们，而是它们出事时把消息推过来。你要做的是反过来——把 CloudEye 的推送地址填到它们后台。

**Q：卡片一直是「未知」状态？**
A：被动模式和反馈联动在**还没收到任何回调 / 推送**之前就是未知，属于正常；收到第一条后才会变色。主动模式（HTTP/TCP）如果一直未知，检查地址是否填对、能否访问。

**Q：「每日时段随机」是干嘛的？**
A：让任务每天在一个时间段内随机执行一次（比如签到），一天只跑一次，随机时间避免固定时刻被识别或撞高峰。

**Q：列表视图 / 网格视图怎么切？**
A：顶栏有个切换按钮，网格是卡片墙，列表是紧凑单行。手机上列表会自动还原成卡片方便看。

---

*有看不懂的地方，或者某个模式想要更详细的例子，随时提。*
